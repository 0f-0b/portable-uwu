[workspace]
members = ["fuzz"]

[package]
name = "portable-uwu"
edition = "2024"

[features]
default = ["alloc"]
alloc = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(fuzzing)'] }

[profile.release]
codegen-units = 1
lto = true
